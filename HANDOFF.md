# Lizard-Spock STEM Academy — Handoff & Progress

Interactive STEM learning site for ages 10+. Friendly-but-factual tone.
**Chosen direction: 1a "Graph-paper field lab"** (warm paper `#faf6ef`, graph-grid bg, ink `#2b2620`, orange accent `#e8590c`, hard 2px ink borders + offset shadows) blended with 1c's friendly "Eight worlds to explore" topic list.

## Design system (apply everywhere)
- Fonts: Archivo (display, 800/900, tight tracking) · Space Grotesk (body) · IBM Plex Mono (annotations, labels, buttons)
- Colors: paper `#faf6ef`, ink `#2b2620`, muted `#6b6156`, border `#d8d0c4`, accent `#e8590c`, white cards `#fff`
- Motifs: `// FIG. 0N — LABEL` mono section headers, 2px ink borders, `4px 4px 0` ink offset shadows on primary buttons, graph-paper background via repeating-linear-gradient (24px)
- Copy tone: friendly but factual, ages 10+. No emoji.

## Site structure
- Everything deployable lives in `public/`. Entry: `public/index.html`.
- **REORGANISED into folders** (was flat): `public/index.{dc.,}html` + `public/progress.{dc.,}html` + `public/support.js` at the root; **all shared JS engines + data in `public/lib/`** (progress-store.js, image-slot.js, elements.js, and every `*-sim.js`/diagram/engine); **chemistry pages in `public/chemistry/`**, **physics pages in `public/physics/`**.
- Reference conventions after reorg: subject pages load `../support.js`, `../lib/<engine>.js`, `../lib/progress-store.js`; root/home link to subjects as `chemistry/…`, `physics/…`; subject→home is `../index.html`, subject→progress is `../progress.html`; **intra-subject links stay bare filenames** (same folder), so progress IDs (`href.replace('.html','')`) are unchanged.
- Pages authored as Design Components (`.dc.html`), copied to plain `.html` names for deployment.
- `Homepage Options.dc.html` (project root) = the 3 original design options; keep for reference.
- ⚠️ NOTE: older progress-log lines below reference the OLD flat paths (e.g. `public/periodic-table.dc.html`); those files now live under `public/chemistry/` or `public/physics/`.

## Curriculum (see `uploads/curriculum-plan.md` — canonical)
- Levels L1 EXPLORER (10–11) → L5 EXPERT (17–18, A-level Yr2 / Adv Higher); every topic/article gets a level badge; site filterable by level/track (GCSE vs Nat5 vs A-level vs Adv Higher, flag syllabus divergence inline).
- Full leveled topic lists for Chemistry, Physics, Biology, Earth Science, Astronomy.
- Interaction-pattern catalog (DIAGRAM, MODEL3D, SIM, BUILDER, ANIM, COMPARE, GRAPH, QUIZ, LAB, CALC, TIMELINE, MAP, SORT, SANDBOX, ARTICLE) — build each engine once, reuse across subjects.
- Build order: 1) DIAGRAM engine 2) SIM engine 3) BUILDER 4) generalize QUIZ from periodic-table; rest as needed.
- Next module suggestion: Physics forces/motion (L1–L3). Math/Engineering/Coding lists still to be written.

## Topic plan (8 worlds)
1. **Chemistry — LIVE**: interactive periodic table (first module)
2. **Physics — LIVE**: 11 interactive labs (`physics.html` map), full L1–L3 curriculum
3. **Biology — LIVE**: `biology.html` world map + 9 live topic pages; **L1 curriculum 100% complete** (classification, life-cycles, habitats, human-body, plants) plus cells, food-webs, genetics; L2–L5 mostly on the bench
4. Math — graphing, geometry, probability (coming soon)
5. **Astronomy — LIVE**: `astronomy/astronomy.html` world map + L1–L3 curriculum (5 topic pages + shared physics/space link)
6. **Earth Science — LIVE**: `earth-science/earth-science.html` world map + full L1–L3 curriculum (7 topic pages + shared atmosphere-climate link)
7. **Engineering — MAP LIVE**: `engineering/engineering.html` world map (L1–L5, all topics benched — no topic pages yet). L3 = GCSE Engineering / National 5 Engineering Science.
8. **Coding — MAP LIVE**: `coding/coding.html` world map (L1–L5, all topics benched — no topic pages yet). L3 = GCSE Computer Science / National 5 Computing Science.

## Periodic table feature list (agreed with user) — ✅ ALL COMPLETE
Verified live in `public/chemistry/periodic-table.dc.html` (2026-07-04):
- [x] Full 118-element grid (18 cols, lanthanide/actinide rows), color-coded by category — `cells` from `elements.js`, `grid-template-columns: repeat(18, …)`
- [x] Click element → detail panel: properties, uses, fun fact — right-hand panel, `selProps` + `selFact`
- [x] Color-mode toggle: category / state-at-temperature — `colorMode` "cat"/"state", CATEGORY/STATE buttons
- [x] Temperature slider (0–6000 K) driving solid/liquid/gas coloring — `tempK` range input, `stateAt(el, tempK)`
- [x] Electron shell diagram (Bohr-style) in detail panel — `<atom-3d>` (3D Bohr, drag-to-spin) + shell config line
- [x] Quiz: "find the element" game with score/streak — quiz mode, LV.1 name / LV.2 masked-fact clue, SCORE/STREAK/SKIP
- ➕ Beyond the agreed list, also shipped: name/symbol/number search box, field-log "N of 118 inspected" tracker, `states-of-matter` + `electron-shells` field-guide cross-links from the panel.

## Progress log
- ✅ Asked design questions; user picked: science-lab feel, warm & energetic, all 8 topics, full periodic-table feature set, coming-soon cards for other topics, mix tone, brand "Lizard-Spock STEM Academy"
- ✅ 3 homepage options built (`Homepage Options.dc.html`); user chose 1a + 1c's friendly topic list
- ✅ `public/elements.js` — full 118-element dataset (mass, category, group/period, melt/boil K, shells, fun fact) + CATEGORIES/STATE_COLORS palettes + `stateAt()` + `gridPos()` helpers
- ✅ `public/periodic-table.dc.html` — full table: 118 tiles, family/state color modes, temp slider (0–6000 K), detail panel (props, Bohr shell diagram SVG, fun fact), quiz mode (score/streak/skip)
- ✅ `public/index.dc.html` — homepage (1a style, hero + teaser grid + "Eight worlds" list, Chemistry LIVE)
- ✅ Copied DCs → `public/index.html`, `public/periodic-table.html`; `public/support.js` present (deploy the `public/` folder as-is)
- ✅ Verifier pass 1: fixed grid overflow (minmax(0,1fr) + overflow-x wrapper) and removed overlapping f-block pointer cells
- ✅ Search box (name/symbol/number, dims non-matches), field-log "N of 118 inspected" (localStorage `lizard-spock-stem.inspected`), quiz levels LV.1 name / LV.2 masked-fact clue
- ✅ `public/atom-3d.js` — <atom-3d> canvas web component: 3D Bohr atom, drag-to-rotate, auto-spin, live orbiting electrons; used in table detail panel + article
- ✅ `public/electron-shells.dc.html` — Field Guide 01 article: shells/energy levels (hotel-floor analogy, 2n² capacities, photon jumps, valence, Bohr-model caveat) + interactive 3D atom playground w/ 6 element presets; linked from detail panel ("READ: HOW ELECTRON SHELLS WORK")
- ⬜ Verify electron-shells + updated periodic-table

- ✅ Nucleus fix: `atom-3d.js` now draws a packed proton(orange)/neutron(grey) 3D cluster (capped ~44 nucleons); symbol moved to corner label
- ✅ `electron-shells.dc.html`: labelled intro carbon-atom diagram (FIG. 01 legend) + `public/photon-jump.js` looping photon-emission animation (FIG. 02, section 04)
- ✅ `public/particle-box.js` — <particle-box> canvas sim (solid/liquid/gas/plasma behaviors)
- ✅ `public/states-of-matter.dc.html` — Field Guide 02 (L1–L2 badge): 4 states + interactive particle box + transitions grid; linked from periodic-table detail panel + cross-linked with FG01
- ⬜ Verify states-of-matter + updated periodic-table
- ✅ Level badges: homepage "LIVE NOW · L1–L3" + levels legend line, FG01 header "LEVEL L2–L3", FG02 "L1–L2", periodic-table READ links carry levels
- ✅ MODULE 2 — Physics forces/motion (L1–L3): `public/force-sim.js` <force-sim> crate-lab canvas sim (push/mass/μ sliders, free-body arrows, static vs kinetic friction, live v–t graph, treadmill ground) + `public/forces-motion.dc.html` page (challenges cards, L1 pushes/pulls → L2 balanced/unbalanced → L3 Newton's laws + graph reading); homepage Physics now LIVE → forces-motion.html
- ✅ Chemistry world map (`public/chemistry.dc.html`): nonlinear dependency graph, all ~38 curriculum topics, L1–L5 bands, live nodes clickable, live-list cards below. Homepage Chemistry card now points here (was periodic-table.html directly).
- ✅ Chemistry L1–L2 sweep (5 new pages, all wired into the graph + liveList):
  - `public/acids-bases.dc.html` + `public/ph-sim.js` — pH slider SIM, indicator colour, neutralisation
  - `public/matter-classify.dc.html` + `public/particle-diagram.js` — element/compound/mixture particle diagrams + click-to-sort game + separating techniques cards
  - `public/word-equations.dc.html` — reaction equation BUILDER (5 reactions: combustion, rusting, neutralisation, displacement, oxide formation)
  - `public/reactivity-series.dc.html` — drag-rank (▲▼) 7-metal reactivity series game with displacement explainer
  - `public/materials-resources.dc.html` — material↔property click-match game + mined/grown/made resource cards
- ✅ Chem L3: `public/bonding.dc.html` + `public/bond-diagram.js` — ionic/covalent/metallic tabbed live diagrams (electron transfer arrow, shared pair, electron sea)
- ✅ Graph layout bug fixed: nodes now flow-wrap per level based on estimated label width (was naive even-spacing causing overlaps on L2/L3 rows) — verified 0 overlaps across all 38+ nodes via DOM measurement
- ✅ Name change: "Lizard-Spock STEM" → "Lizard-Spock STEM Academy" across all live pages + HANDOFF
- ⬜ Verify all new pages load cleanly + chemistry graph layout holds with 8 live nodes
- ✅ Chem L3: `public/bonding.dc.html` + `public/bond-diagram.js` — ionic/covalent/metallic tabbed live diagrams (electron transfer arrow, shared pair, electron sea)
- ✅ Chem L3: `public/rates-of-reaction.dc.html` + `public/rate-sim.js` — collision-theory flask sim (concentration/temp/surface-area sliders, live gas-collection + rate sparkline)
- ✅ Chem L3: `public/energetics.dc.html` + `public/energy-profile.js` — exo/endothermic energy-profile diagram (Eₐ + ΔH labelled), tabbed
- ✅ Chem L3: `public/electrolysis.dc.html` + `public/electrolysis-sim.js` — live cation/anion migration + discharge animation between labelled electrodes
- ✅ Progress tracking system (local-only, Firebase-ready): `public/progress-store.js` (localStorage, visited/completed API, TOPICS registry), wired into all 13 live pages' componentDidMount + completion conditions; `public/chemistry.dc.html` map shows green ✓ checkmarks; new `public/progress.dc.html` profile page (stats, % bar, per-topic status, disabled "Sign in with Google" stub)
- ✅ `firebase.md` — step-by-step for user to create Firebase project, enable Google auth + Firestore, set security rules, and hand back the web config so real cross-device sign-in can be wired in
- ⬜ Verify all 4 new L3 pages load cleanly + progress tracking end-to-end (spot-checked manually, looks correct)
- ✅ Chem L3 sweep complete (5 more pages): `periodic-trends.dc.html` (real-data bar charts: radius/reactivity/melting point), `quantitative-chem.dc.html` (mass↔moles calculator, 6 substances), `organic-intro.dc.html` + `chain-diagram.js` (alkane/alkene chain builder), `chemical-analysis.dc.html` (flame-test match game + gas test reference), `atmosphere-climate.dc.html` + `atmos-pie.js` (composition donut + greenhouse effect toggle)
- ✅ All wired into chemistry map, progress-store TOPICS, and liveList — **L1–L3 chemistry curriculum is now 100% covered**, 18 live topics total (17 chem + 1 physics)
- ✅ **PHYSICS L1–L3 FULLY BUILT (module 2 → full subject)**. `public/physics.dc.html` map page (chemistry-map visual system: 37 nodes, L1–L5 bands, 0 overlaps, live nodes orange + green ✓). 10 new topic pages, each spanning its levels with a bespoke canvas sim, all wired into progress-store TOPICS + the physics map + the homepage Physics card (now → `physics.html`). New pages & sims:
  - `simple-machines` + `lever-sim.js` — lever/ramp/pulley bench, live mechanical advantage + effort readout
  - `energy` + `energy-flow.js` — Sankey energy-transfer diagram (6 devices), efficiency; L1 sources, L2 stores, L3 conservation
  - `electricity` + `circuit-sim.js` — animated circuit, glowing bulbs, flowing-charge dots, live Ohm's law; single/series/parallel modes
  - `magnetism` + `field-sim.js` — dipole field-line tracer (bar magnet) + electromagnet (current/turns → strength, paperclips)
  - `sound` + `sound-wave.js` — live oscilloscope: freq→pitch, amp→volume, transverse trace + longitudinal air-particle compressions
  - `light-waves` + `ray-sim.js` — reflection (i=r) + refraction (Snell, water/glass/diamond) ray box; + CSS EM-spectrum figure (FIG. 02)
  - `momentum` + `collision-sim.js` — 1D collision track, restitution slider (elastic/partly/sticky), live momentum-conservation tally
  - `radioactivity` + `decay-sim.js` — 400-nucleus decay grid + decay curve w/ half-life markers, α/β/γ cards
  - `pressure` + `pressure-sim.js` — gas-in-a-box: volume+temp sliders → wall-hit rate → pressure (Boyle/Gay-Lussac), particle model + density
  - `space` + `orbit-sim.js` — satellite orbit (Kepler: bigger orbit = slower) + animated inner solar system; day/night, seasons, star life cycle
- ✅ `forces-motion` nav + next-links repointed to `physics.html` map for consistency.
- ⬜ Verify all 11 physics pages load cleanly end-to-end (spot-checked each sim manually — lever, circuit, field lines, refraction 40°→29°, decay 51%@1 half-life, momentum conserved, orbits all render correctly).

- ✅ Chem L3: `public/salts.dc.html` + `public/titration-curve.js` — salt-building (metal/base/carbonate partners) + live titration pH-curve. **This was the last L3 topic — L1–L3 chemistry curriculum is now literally 100% complete: 19 live chem topics + 1 physics module.**
- ✅ `public/particle-diagram.js` reworked twice: (1) real substance examples (Au/O₂ element, NaCl compound, air's N₂/O₂/Ar mixture) with in-canvas legends per CLAUDE.md figure standard; (2) bonds redrawn as touching/overlapping circles (space-filling style, matches BBC Bitesize/knowledge-organiser convention) instead of stick lines; Element diagram now shows two clearly divided, individually-legended examples (metal/unbonded vs. diatomic gas/bonded) so they don't read as a mixture. `matter-classify.dc.html`: added missing `techniques` data (verifier caught cards rendering empty), added `// FIG. 0N` captions above all three particle diagrams.
- ✅ `matter-classify` separating-techniques section (2026-07): rebuilt from static cards into an **interactive animation stage** — new reusable `public/lib/separation-sim.js` (`<separation-sim method="…">` looping canvas) animates all four methods (filtration: funnel/residue/filtrate; evaporation: dish/flame/vapour/salt crystals; distillation: flask→condenser→pure-liquid beaker; chromatography: solvent front rising + ink splitting into colours by solubility). Methods stacked on the right, hover/click plays the selected one on the left (FIG. 05), all labelled per CLAUDE.md figure standards.
- ✅ `acids-bases` neutralisation section (2026-07): text-only paragraph replaced with an **interactive ion beaker** — new `public/lib/neutralise-sim.js` (`<neutralise-sim>` canvas) draws free H⁺/OH⁻ ions, the H₂O molecules formed when they pair, and Na⁺/Cl⁻ spectator (salt) ions, with the liquid tinted by pH. Controls: ADD ACID / ADD ALKALI / RESET; live readout (ACIDIC→NEUTRAL→ALKALINE, pH, free-ion counts); completes on full neutralisation. (Page order confirmed good: leads with the pH experiment, prose follows — learn-by-doing.)
- ✅ `states-of-matter` + `state-fsm.js` reworked (2026-07): removed the 4 redundant state buttons (nodes are clickable); FSM redrawn so **energy-adding transitions (melt/boil/ionise/sublime) arc across the TOP half** and **energy-removing (freeze/condense/recombine/deposit) across the BOTTOM**, with ▲/▼ energy-direction hints and much bigger arrows; next-links repointed to the next topic (matter-classify) + a level-up (electron-shells); added a References block.
- 📐 **CLAUDE.md rewritten into 4 enforced standards** (apply to all future pages): §1 figure/table/graph labelling (numbered FIG/TABLE/GRAPH captions + legends + axis units), §2 minimum sizes (arrows ≥2px stroke / ≥10px head, hit targets ≥44px, labels ≥9px), §3 references block (Wikipedia + BBC Bitesize) on every content page, §4 next-topic linking rule (forward, never same/back). Site-wide rollout of §1–§4 to existing pages = todos 21–24.
- ✅ **CHEMISTRY L4–L5 BUILT (12 new pages).** All wired into the chemistry map (bench nodes → live orange/green, 38 nodes, 0 overlaps verified), `progress-store.js` TOPICS, and the map's liveList; each copied to its deploy `.html`. Every page follows the house pattern (nav → title → interactive → theory → next-links) with completion tracking. Physics values spot-checked correct on each.
  - **L4:** `stoichiometry` (step-by-step reacting-mass calculator, 24 g Mg→40 g MgO ✓) · `equilibria` (Le Chatelier position-bar for the Haber process, pressure/temp → yield%) · `redox` (oxidation-number auditor, MnO₄⁻→Mn +7 ✓; OIL RIG + half-equations) · `enthalpy` (bond-energy ledger, H₂+Cl₂→2HCl = −184 kJ/mol ✓ + Hess cycle figure) · `further-organic` (esterification builder: acid+alcohol→named ester + functional-group table) · `nuclear` (reuses `decay-sim.js`; nuclear equations, fission/fusion, E=mc²)
  - **L5:** `thermodynamics` (ΔG=ΔH−TΔS calc + temperature crossover number-line, CaCO₃ ΔG=+130@298K, cross 1106K ✓) · `kinetics` (+ `rate-plot.js`: rate–conc graph for orders 0/1/2, ×4 on doubling ✓) · `equilibrium-constant` (Kc calculator for H₂+I₂⇌2HI) · `electrochemistry` (cell builder from electrode potentials, Mg/Pb = 2.24 V ✓ + electrochemical series) · `transition-metals` (ion colour bench, [Cu(NH₃)₄]²⁺ deep blue) · `acid-base-equilibria` (weak/strong-acid pH calc via Ka, ethanoic 0.1 M → pH 2.88 ✓)
  - New reusable sim: `public/rate-plot.js` (`<rate-plot>` rate-vs-concentration canvas). `mechanisms`, `spectroscopy`, `aromatic` remain the only bench (not-built) chemistry nodes — **L1–L5 chemistry is otherwise 100% live (31 topics).**
- ✅ **CHEMISTRY 100% COMPLETE — last 3 L5 nodes built** (`mechanisms.dc.html`, `spectroscopy.dc.html`, `aromatic.dc.html`), all self-contained DCs (no new .js), wired into `chemistry.dc.html` (nodes now live + liveList cards), `progress-store.js` TOPICS, deploy `.html` copies synced. **Every chemistry node on the map is now live — 34 chem topics.**
  - `mechanisms` — mechanism-player stepper: free-radical substitution, electrophilic addition, SN2; prev/next steps + dots, notation legend (•, δ±, curly arrow), cast cards. Completion after 2 mechanisms viewed to end.
  - `spectroscopy` — mass-spec / IR / ¹H-NMR bench for ethanol · propanone · ethanoic acid; all spectra SVG-plotted with labelled axes+units (m/z, cm⁻¹ high→low, δ ppm), per-technique readout rows. Completion after 2 techniques.
  - `aromatic` — benzene model toggle (delocalised circle ↔ Kekulé double bonds, computed hexagon SVG + legend) + electrophilic-substitution bench (nitration/bromination/FC-alkylation/FC-acylation/sulfonation with reagents/conditions/electrophile/product). Completion after 3 reactions.
- ℹ️ NOTE re "l3 physics": Physics L1–L3 is ALREADY 100% built — every L1/L2/L3 node on `physics.html` has a live page. The only physics bench nodes are L4–L5 (kinematics, dynamics, SHM, fields, thermal, etc.). Confirm with user whether to build Physics L4–L5 next.
- ✅ BREADCRUMBS: every page's top-nav crumb is now a real navigation breadcrumb — `HOME / SUBJECT / context` with HOME (→index.html) and SUBJECT (→chemistry.html/physics.html) as links; map pages show `HOME / SUBJECT WORLD MAP`. Applied across all 46 pages (both `.dc.html` + deploy `.html`, 92 files) via script. Crumb style unchanged (mono 12px #6b6156, hover #2b2620).
- ✅ **BIOLOGY — STARTED (module: world map + first topic sweep, 2026-07-04).** New `public/biology/` folder. `biology.dc.html` world map (chemistry/physics map visual system: 35 nodes across L1–L5 bands, nonlinear "learn this first" dependency graph, live nodes orange, 0 overlaps). 4 live topic pages built, each self-contained (no new .js), following the house pattern (nav → title → interactive → theory → next-links), wired into `progress-store.js` TOPICS + the biology map liveList + node hrefs, with completion tracking, and copied to deploy `.html`:
  - `cells` (L2–L3) — clickable plant/animal cell DIAGRAM (organelles as simple positioned shapes + labels + legend per CLAUDE.md); tabbed animal↔plant; info panel with job + analogy; plant-only extras summary. Completion after 6 parts inspected.
  - `classification` (L1–L2) — dichotomous-key SORT game: mystery organism, YES/NO branches down a real vertebrate/invertebrate key to its group; score/streak; five-kingdoms reference. Completion after 3 solved. (Serves both the L1 "Living things" and L2 "Classification & variation" nodes.)
  - `food-webs` (L2–L3) — woodland food-web BUILDER/SIM: click any organism to remove it, connected species show ▲/▼ ripple; colour-coded trophic legend; pyramid-of-energy FIG (÷10 per level). Nodes positioned by % so they track the viewBox arrows at any width.
  - `genetics` (L3) — live Punnett-square CALC: pick both parent genotypes across 3 pea traits (flower/seed/height), 2×2 grid fills, phenotype (3:1) + genotype (1:2:1) ratios computed, allele key + glossary.
- ✅ Homepage Biology card → LIVE → `biology/biology.html`. Breadcrumbs on all biology pages are real links (HOME → ../index.html, BIOLOGY → biology.html).
- ✅ **BIOLOGY L1 COMPLETE (2026-07-04).** All 5 L1 nodes on the biology map are now live, each a self-contained DC (no new .js) built for maximum interactivity, wired into `progress-store.js` TOPICS + biology map node hrefs + liveList, deploy `.html` synced:
  - `classification` (already built) — dichotomous-key game (serves L1 "Living things" + L2 "Classification & variation"). **Updated 2026-07: five-kingdoms section rebuilt as an interactive explorer** — colour-coded kingdom list on the left (all on one level), hover/click reveals a detail panel on the right (cells / feeding / movement / where found / examples); the term **taxonomy** is now introduced explicitly and the kingdom→phylum→…→species ranks are named. FUTURE (higher level): a zoomable hierarchical taxonomy-tree viewer with two-organism path comparison + common-ancestor highlight (too big for one screen at L1) — see todo.
  - `life-cycles` (L1) — clickable **circular life-cycle wheel** (butterfly/frog/plant/chicken) with ▶ PLAY auto-advance + prev/next stepper, PLUS a "put the stages in order" click game (shuffle/reset, streak). Completion after ordering a full cycle.
  - `habitats` (L1) — habitat explorer (5 habitats: arctic/desert/rainforest/ocean/grassland, condition gauges + resident animal + its adaptations) PLUS an "which habitat needs this adaptation?" quiz (score/streak). Completion after 4 correct.
  - `human-body` (L1) — clickable body figure with **ORGANS ↔ SKELETON** toggle (organs: brain/lungs/heart/stomach/intestines; skeleton: skull/ribcage/spine/pelvis/femur; simple-shape silhouette + labels + legend), PLUS a five-senses match game + balanced-plate food-group cards. Completion after 6 parts explored.
  - `plants` (L1) — clickable plant-parts diagram (flower/leaves/stem/roots) PLUS a **grow-a-plant SIM**: water/light/warmth sliders drive a live plant that greens & flowers when balanced, yellows/droops/browns when not (geometric-mean health score + worst-factor advice + reactive sky/sun), plus the photosynthesis word equation. Completion after inspecting all 4 parts.
- ✅ **BIOLOGY L2 COMPLETE (2026-07-05).** The 4 remaining L2 bench nodes are now live — self-contained DCs (no new .js), house pattern (nav → title → interactive → theory → §3 references → forward next-links), wired into `progress-store.js` TOPICS + biology map node hrefs + liveList, deploy `.html` synced:
  - `digestion` (L2) — "follow the food" gut-journey map (6 clickable stops + ▶ play bolus animation + per-stop detail), TABLE 01 of the 3 digestive enzymes, and a "name that nutrient" quiz (7 groups). Completion after 4 quiz correct.
  - `reproduction` (L2) — clickable flower cross-section (8 labelled parts, male/female/helper legend), FIG 03 pollen→new-plant 4-step stepper, sexual-vs-asexual compare cards + "sexual or asexual?" sort game. Completion after 4 correct.
  - `evolution-intro` (L2) — peppered-moth natural-selection SIM: clean/sooty bark toggle, birds eat the moths that stand out, population repopulates by survivor colour ratio; live population bar + GRAPH 01 (% dark over generations) + 4-step natural-selection figure + industrial-melanism history. Completion after 5 generations.
  - `body-systems` (L2) — 6-system selector lighting up schematic organs on a body silhouette (circulatory/respiratory/digestive/nervous/skeletal/muscular, each with organs + partner-system), FIG 03 levels-of-organisation ladder (cell→tissue→organ→system→organism), "which system?" match game. Completion after 4 correct.
- ⬜ Remaining biology: all **L3** except cells/food-webs/genetics, and all **L4–L5** (L2 now fully live). Chemistry 100% done; Physics L4–L5 bench; Earth Science/Astronomy/Math/Engineering/Coding still to start — see uploads/curriculum-plan.md

- ✅ **FULL SITE AUDIT against the 4 CLAUDE.md standards (2026-07-05).** Swept all 56 content pages (biology 12, chemistry 33, physics 11; maps/index/progress excluded) + their deploy `.html` twins + shared `lib/*.js` sims:
  - **§3 References** — was present on only 5 pages; now on **all 56**. Each page got a tailored `// REFERENCES & FURTHER READING` block (2–4 Wikipedia/BBC-Bitesize links, blue `#2a6fb0` `↗`, `target=_blank rel=noopener`) + the exam-board cross-check line, inserted before the next-nav. periodic-table (app layout, no column) got a bespoke band before its footer.
  - **§4 Next-topic** — legacy chem/physics pages had the **subject map as the PRIMARY** next button (violation); rebuilt every legacy nav to a forward `NEXT TOPIC` primary (next page in the per-level chain) with the map demoted to a secondary link. Biology `food-webs`→classification and `genetics`→cells were **backward** primaries — repointed forward (food-webs→genetics; genetics is a leaf → framed "KEEP EXPLORING / Explore the Biology map"). Leaf pages (aromatic, space) use the same forward-framed map fallback.
  - **§1 Captions** — 7 pages had ad-hoc eyebrows ("// EXPERIMENT", "// CHAIN BUILDER", "// CALCULATOR", etc.) instead of numbered captions; relabelled to `// FIG. 01 —` / `// TABLE 01 —`. **Every content page now carries ≥1 numbered FIG/GRAPH/TABLE caption** (verified by script).
  - **§2 Minimum sizes** — bumped under-floor arrowheads to ≥11px in `force-sim` (9→12), `ray-sim` (8→11), `collision-sim` (8→12); added a missing arrowhead to `orbit-sim`'s velocity vector; thickened hairline field-line "arrows" in `field-sim` (1.3/1.4→2px); fixed sub-9px labels in `neutralise-sim` (7→9px) and `rate-sim` (8.5→9px). Strokes were already ≥2px elsewhere.
  - Transform done via idempotent scripts (guarded on existing refs); both `.dc.html` and deploy `.html` written in the same pass, so deploy is in sync. Spot-checked salts (chem tail), forces-motion (physics sim + arrows), digestion/evolution (bio) rendering clean.
  - ⬜ FOLLOW-UP (deeper §1, not yet swept): confirm every GRAPH labels both axes **with units** and every colour/shape has a legend, page by page — captions exist but per-figure legend/axis-unit completeness wasn't exhaustively audited.
- ⬜ TODO: review site content against exam board specs via BBC Bitesize science hub (https://www.bbc.co.uk/bitesize/subjects/zs6hvcw) — cross-check curriculum coverage & terminology against AQA/Edexcel/OCR etc.

## Firebase (Google auth + cross-device progress) — LIVE
- Config is in `public/progress-store.js` (real project `lizard-spock-stem`). Dynamically imports Firebase Auth + Firestore v10 ESM from CDN — no build step, loads on every page since every page already includes `progress-store.js`.
- Flow: guest = localStorage only (unchanged). Sign in with Google (button on `/progress.html`) → merges local progress into `progress/{uid}` Firestore doc (earliest-visited/latest-completed wins), then live-syncs via `onSnapshot` — so progress follows the account across devices/browsers.
- New API on `Progress`: `signIn()`, `signOutUser()`, `onAuthChange(cb)`, `authUser()`.
- ⚠️ If sign-in silently fails: the current preview/production domain needs adding under Firebase Console → Authentication → Settings → Authorized domains (see `firebase.md` step 2.3).
- Firestore security rules (per `firebase.md`) must be applied in console — not something this tool can set.

- ~~TODO: build a `chemistry.dc.html`-style index/map page for every other subject~~ ✅ ALL DONE — Physics (`physics.html`), Biology (`biology.html`), Earth Science (`earth-science/earth-science.html`), Astronomy (`astronomy/astronomy.html`), Math (`math/math.html` — map only, all topics benched; see "Math — MAP + BUILD PLAN" section below for the full implementation spec).
- Physics remaining for full curriculum: all of L4–L5 (kinematics/SUVAT, dynamics & circular motion, impulse, work-energy-power, circuit analysis, waves & radiation, Our Dynamic Universe; then SHM, capacitors & fields, thermal physics, nuclear & particle, quantum, astrophysics, rotational dynamics) — all present as bench nodes on the physics map, no pages yet.

- 🟡 HERO IN PROGRESS: `index.dc.html` hero rebuilt — left column keeps the headline/CTAs; the mini-table teaser was replaced with a **faded pastel periodic-table wallmark** (full-width absolute layer, real 5-period table shape, `bgCells` in renderVals, opacity 0.2) with a **drop-in dragon image-slot** floating over its right side (`<image-slot id="hero-dragon" fit="contain">`, `public/image-slot.js` copied into public/). Caption "FIG. 00 — Ls, THE PERIODIC DRAGON". Deploy copy `index.html` synced.
  - ⬜ **BLOCKED on the actual dragon art** — no image-gen/stock tool in this env. Slot shows a placeholder until filled. Editor drops don't persist to Firebase static hosting, so for the LIVE site the fire-breathing robot dragon must be a committed file: user uploads a transparent PNG → save to `public/hero-dragon.png` → set `src="hero-dragon.png"` on the x-import → re-copy to `index.html`.
- (old TODO, superseded) homepage hero image — a fire-breathing robot dragon, with a faded/pastel periodic table pattern as the background behind it. Needs an actual image asset (illustration or generated art) — not something to fake with CSS/SVG per house style (avoid drawing imagery in SVG). Ask user for the image or generate/source one before implementing.

- ✅ **BIOLOGY L3 COMPLETE (2026-07-05).** The 6 remaining L3 bench nodes are now live — self-contained DCs (no new .js), house pattern (nav → title → interactive → theory → §3 references → forward next-links), wired into `progress-store.js` TOPICS + biology map node hrefs + liveList (all 6 verified orange/live on the map), deploy `.html` synced. **Biology is now live through L3** (18 topic pages); only L4–L5 remain on the bench.
  - `bioenergetics` (L3) — pondweed photosynthesis lab: light/CO₂/temperature sliders → animated O₂ bubbles + live rate readout + limiting-factor call-out; GRAPH 01 rate-vs-light with plateau; photosynthesis/respiration word equations + aerobic-vs-anaerobic TABLE. Completes on reaching a high rate.
  - `organisation` (L3) — enzyme lock-and-key animation stage (substrate binds → products released; amylase/pepsin tabs), temp+pH sliders with an activity bar that DENATURES past optimum; GRAPH 01 activity-vs-temperature; levels-of-organisation ladder + digestive-enzymes TABLE. Completes after both enzymes run near optimum.
  - `infection` (L3) — herd-immunity outbreak SIM: 130-person grid, % vaccinated slider, START OUTBREAK spreads S→I→R through neighbours; live counters + GRAPH 01 epidemic curve; pathogen-types TABLE + immune-defence steps. Completes when an outbreak resolves.
  - `homeostasis` (L3) — thermoregulation negative-feedback SIM: outside-temp slider vs a body holding 37 °C, sweating/shivering responses, HOMEOSTASIS ON/OFF toggle (off = core drifts into danger bands); GRAPH 01 core-temp-over-time with set-point line; reflex-arc FIG + insulin/glucagon FIG. Completes after experiencing both extremes.
  - `natural-selection` (L3) — antibiotic-resistance evolution SIM: petri dish coloured by resistance, "give antibiotic → survivors breed" advances generations, mean resistance climbs; GRAPH 01 mean-resistance bar chart; 4 steps of natural selection + evidence cards. Distinct from the L2 peppered-moth sim. Completes at generation 5.
  - `ecology` (L3) — predator-prey Lotka–Volterra SIM (rabbits/foxes meadow + live two-series oscillation graph, predation-rate slider) AND a quadrat-sampling estimator (random 1 m² quadrats → scaled estimate vs true count). Completes after 3 quadrat samples.
- 📝 **SITE REVIEW → `NOTES-interactivity-ideas.md`** (2026-07-05): menu of extra animation/interactivity ideas from a full walk-through, grouped by effort (quick wins like animated map edges + progress-page rings + `prefers-reduced-motion`; per-page upgrades like a cells "at work" mode, a reaction animator, Web-Audio mic input on the sound oscilloscope; bigger builds like a results "lab notebook" and end-of-topic checks). Not committed — a pick-from list.

## Known bugs / future updates
### Fixed
- ✅ `periodic-table.dc.html`: `{{ atomComposition }}` hole never resolved (console warning) — the var was only declared with `var` *inside* the `if (m)` block, so before `elements.js` loaded it was `undefined` (unlike `shellConfig`, which had a `""` default). Fixed by hoisting it into the top `let` list with a `""` default. Redeployed `periodic-table.html`.
- ✅ (earlier) nucleus proton/neutron cluster, electron-shells intro atom diagram, photon-jump animation, and the states-of-matter page — all DONE, see progress log.
- Bug sweep (this pass): opened periodic-table, chemistry map, electron-shells, salts, electrolysis, rates-of-reaction, periodic-trends, progress, states-of-matter — all clean (no console errors/warnings) after the fix above.

### Open ideas

## ENGINEERING & CODING MAPS (2026-07-12) — built, benched, live on homepage
Both subjects now have full L1–L5 world maps (`public/engineering/engineering.html`, `public/coding/coding.html`) using the standard map engine (flow-wrap layout, strand border-left colours, dependency edges, "coming soon" planned-topic cards). All nodes benched (white, no topic pages yet). Homepage cards flipped live → link to the maps (levels label "MAP"). Each map header states the L3 qualifications it targets:
- **Engineering L3** = GCSE Engineering (e.g. AQA 8852) · National 5 Engineering Science (SQA) · L2 vocational (Cambridge Nationals / BTEC Tech Award in Engineering). Strands: Mechanical, Electrical/Electronic, Materials, Design & Make, Systems & Control. L3 nodes: design process & brief, engineering materials, mechanical systems (gear ratios), electronic systems & logic, structures/loads/moments, manufacturing processes, systems & control, engineering drawing.
- **Coding L3** = GCSE Computer Science (AQA 8525 / OCR J277 / Edexcel) · National 5 Computing Science (SQA) · iGCSE. Strands: Programming, Algorithms & Data, Computer Systems, Networks, Theory & Impact. L3 nodes: programming (data types/strings), subroutines & structure, data structures, search & sort algorithms, Boolean logic & gates, data representation, computer systems (CPU/memory), networks & protocols, cyber security & ethics.
- TODO (not built): topic pages for either subject. Same recipe as other subjects — build DC + deploy .html, register progress IDs, wire node hrefs. Interactivity ideas are in each map's planned-topic cards (block-coding sim, gear-ratio builder, bridge-truss loader, logic-gate wiring, binary flipper, packet-routing sim, search/sort visualiser, etc.).

## Next ideas (not committed)
- Element "collection" progress (localStorage) — mark elements you've inspected
- Quiz difficulty levels (name→click, clue→click, symbol→name)

## Math — MAP + BUILD PLAN (2026-07-06) — pages to be implemented next (Opus)
Map is LIVE at `public/math/math.html` (`math.dc.html` + own `support.js` copy) — 15 nodes, all benched (white). Homepage Math card is still **coming soon**: flip it to `live: true, levels: "L1–L3", href: "math/math.html"` in `index.dc.html` only when the first topic pages ship (then re-copy to `index.html`). Curriculum plan (`uploads/curriculum-plan.md`) deliberately excluded Math — the leveled list below is the canonical plan.

### BUILD PROGRESS — L1–L3 COMPLETE (2026-07-06)
- ✅ ALL 11 L1–L3 topics SHIPPED: `algebra-basics`, `number-line`, `coordinates`, `quadratics`, `trigonometry`, `probability`, `fractions-percent`, `statistics`, `shapes-angles`, `data-graphs`, `geometry-measure`. Every node wired live (orange) on the map with its strand colour; every progress ID registered; every deploy `.html` copy synced.
- ✅ Homepage Math card flipped LIVE → `math/math.html` in `index.dc.html` + `index.html`.
- ✅ Formula-sheet cards now link to their real topic pages where shipped (`SHIPPED` map in `formula-sheet.dc.html`'s `renderVals()`: quadratics, trigonometry, shapes-angles, geometry-measure, fractions-percent, probability, coordinates, statistics) — the rest still show "(coming soon)" since their owning L4/L5 topics aren't built.
- Highlights: `algebra-basics` balance-scales solver (3 tiers, illegal-move warning); `quadratics` live projectile game + discriminant narration; `trigonometry` draggable Pythagoras square-proof (verified a²+b²=c² holds while dragging) + SOHCAHTOA quiz; `probability` spinner law-of-large-numbers convergence (verified via 1000-spin screenshot) + two-dice heatmap; `statistics` draggable mean/median dot plot + scatter/correlation + box plot (all three reuse one dataset); `geometry-measure` parallel-lines angle-pair explorer + parallelogram shear proof + fence-optimisation puzzle.
- ⬜ REMAINING: L4/L5 topics (sequences-series, exp-logs, trig-advanced, functions-calculus, vectors, distributions, mechanics-kinematics, calculus, numerical-methods, normal-hypothesis, complex-proof) — fully benched, IDs not yet registered. Full specs are further down this section, unchanged.
- ⚠ ENGINEERING PITFALLS discovered this session (read before building more SVG-heavy pages):
  1. A `{{ hole }}` as the sole/dynamic text content of an SVG `<text>` element gets wrapped by the runtime in an HTML `<span class="sc-interp">`, invalid inside SVG's namespace — silently fails to render (no console error; confirmed via `getBBox()` returning all-zero). FIX: wrap the `<svg>` in a `position: relative` div and render dynamic labels as absolutely-positioned HTML `<div>`s on top (percentage-based `left`/`top` from the viewBox), keeping only lines/shapes/circles in the SVG. Static (non-interpolated) SVG text seems unaffected.
  2. `{{ }}` holes do NOT support inline arithmetic (e.g. `{{ shBx-14 }}` silently fails/renders nothing useful) — always compute derived values in `renderVals()` and expose them by name.
  3. Not yet audited: whether earth-science/astronomy pages from earlier sessions have the SVG-text bug — spot-check if a shipped diagram's labels look missing.
- Drag interactions across pages use a consistent pattern: `onMouseDown` on the draggable element sets a `dragIndex`/`dragging` flag in state; a container-level `onMouseMove`/`onTouchMove` computes position via `e.target.closest("svg").getBoundingClientRect()` and the event's clientX/Y mapped into the SVG's viewBox coordinate space; `onMouseUp`/`onMouseLeave`/`onTouchEnd` clears the flag. Reuse this rather than reinventing per page.

### SYLLABUS AUDIT (2026-07-06) — L3 gap analysis + 6 NEW topics added
Checked against the official 6-strand GCSE/National 5 spec (Number · Algebra · **Ratio, proportion & rates of change** · Geometry & measures · Probability · Statistics — confirmed via AQA 8300 spec, common to AQA/Edexcel/OCR). The original 11-topic L1–L3 build did **not** reach full coverage — Ratio & Proportion was missing as a whole strand, and Algebra/Geometry/Probability/Statistics each had real sub-topic gaps. Added 6 new benched (white) L3 nodes to the map to close this — **not yet built**, specs below. IDs NOT yet registered in `progress-store.js` (do so when building).

- **`ratio` — "Ratio & Proportion"** (Number/Algebra strand — was 100% missing). FIG 01 ratio-splitting bar: total amount + ratio (a:b:c) sliders: a bar divides live into coloured segments with each segment's amount labelled. FIG 02 direct vs inverse proportion race: two linked sliders/graphs — y=kx (line through origin) vs y=k/x (reciprocal curve) — drag x, watch y respond on both, narrate "as one doubles, the other doubles / halves". FIG 03 compound measures calculator: speed/distance/time triangle AND density/mass/volume triangle (cover-up method), draggable to reveal the rearranged formula. COMPLETE: solve a direct + an inverse proportion problem. Cross-link: compound interest already in `fractions-percent`.
- **`seqineq` — "Sequences, Simultaneous Equations & Inequalities"** (Algebra strand gaps). FIG 01 sequence pattern-spotter: growing dot/tile pattern (linear then quadratic) alongside its term number; nth-term slider builds the algebraic rule term-by-term; includes special sequences (triangular/square/cube/Fibonacci) as a gallery. FIG 02 simultaneous equations as crossing lines: two draggable/slidable linear graphs, intersection point IS the solution, elimination method shown step-by-step alongside. FIG 03 inequality number-line & region shader: drag open/closed circles on a number line for 1-variable; for 2-variable, shade the correct side of a line on a graph with a test-point click. COMPLETE: solve 1 sequence nth-term + 1 simultaneous pair + shade 1 region correctly.
- **`transform3d` — "Transformations & 3D Shapes"** (Geometry strand gaps). FIG 01 transformation playground: a shape + 4 buttons (reflect/rotate/translate/enlarge); reflect drags a mirror line, rotate drags a centre + angle dial, translate drags by a column vector, enlarge drags a centre + scale-factor slider (incl. negative/fractional SF for Higher); before/after both shown, congruent vs similar labelled correctly per transformation type. FIG 02 net-folder: click a net (cube/cuboid/triangular prism/cylinder/square pyramid) and an animated fold assembles the 3D solid; face count + surface-area sum shown face-by-face; volume formula alongside with a fill-animation (litres pouring in) for prisms/cylinders. FIG 03 similar-shapes scale-factor: two similar shapes, drag one's scale slider, corresponding sides/areas(SF²)/volumes(SF³) all update — the SF² and SF³ relationship is the payoff moment.
- **`circletheorems` — "Circle Theorems (Higher)"** (Geometry, Higher-tier only — flag inline per house divergence rule; National 5 doesn't require proofs but Higher GCSE/Nat 5 Applications does touch some). FIG 01 draggable circle-theorem prover: pick a theorem from a tab list (angle at centre = 2× at circumference; angles in the same segment; angle in a semicircle = 90°; cyclic quadrilateral opposite angles sum 180°; tangent ⊥ radius; two tangents from a point are equal; alternate segment theorem) — points draggable around the circumference, the relevant angles auto-update and the relationship holds live no matter where you drag. COMPLETE: verify all 7 theorems hold by dragging each into ≥2 configurations.
- **`combinedevents` — "Tree & Venn Diagrams"** (Probability strand gap — combined events). FIG 01 probability tree builder: 2 (then 3) events with editable branch probabilities (must sum to 1 per node); click a path to compute its product; AND/OR toggle sums the right paths; with-replacement vs without-replacement toggle changes second-branch probabilities live (classic "2 balls from a bag" setup). FIG 02 Venn diagram sandbox: 2 then 3 overlapping circles for a scenario (e.g. "plays football / plays chess"); drag counts into regions; click a set-notation expression (A∩B, A∪B, A', etc.) and the matching region highlights; total always checks against the universal set. COMPLETE: build 1 tree computing a correct combined probability + correctly shade 3 set-notation regions.
- **`groupeddata` — "Histograms & Cumulative Frequency"** (Statistics strand gap — grouped/continuous data, Higher-weighted). GRAPH 01 histogram builder: given a grouped frequency table with UNEQUAL class widths, drag/compute frequency density (freq ÷ width) per bar — bar AREA (not height) represents frequency, the common misconception to defeat visually (equal-looking-height bars with different widths clearly hold different counts once shaded by area). GRAPH 02 cumulative-frequency curve: running total plotted as an S-curve from the same table; drag a horizontal line to read off median/quartiles/IQR; box plot draws itself alongside from the same reads (closes the loop with the existing `statistics` page's box plot). COMPLETE: read the median + IQR off a cumulative frequency curve correctly.

### BUILD PROGRESS — GAP-FILL TOPICS (resume here)
- ✅ SHIPPED: `ratio.html` (Ratio & Proportion) — bar-split by ratio (live segments), direct-vs-inverse proportion race (y=kx line vs y=k/x curve, linked sliders), speed & density cover-up triangles, 4-question direct/inverse quiz (completes at 2 correct). Node wired live on map, progress ID registered, deploy copy synced.
- ✅ SHIPPED: `seqineq.html` (Sequences, Simultaneous Eqns & Inequalities) — sequence explorer (linear/quadratic/special tabs incl. triangular/square/cube/Fibonacci, jump-to-term-N), simultaneous equations as crossing lines (4 sliders, live elimination + solution readout, verified x=3,y=1 by hand), 1-variable number-line inequality (open/closed dot, 4 operators) + 2-variable shaded-region grid with click-to-test-a-point. Completes after exploring all 3 sequence tabs + moving both lines + 2 region test-clicks. Node wired live, progress ID registered, deploy copy synced.
- ✅ SHIPPED: `transform3d.html` (Transformations & 3D Shapes) — transformation playground (reflect/rotate/translate/enlarge mode tabs, live before/after triangle, correct congruent-vs-similar labelling incl. negative/SF=±1 edge cases); net-folder (cube/cuboid/triangular prism/square pyramid, flat-net ↔ 3D-view toggle, face/edge/vertex counts, face-area breakdown, surface area + volume with a fill-bar); similar-shapes SF²/SF³ demo (shared slider drives both a scaling square and a scaling cube, verified 2²=4/2³=8 numerically). Completes after exploring all 4 transform modes + 2 solids. Node wired live, progress ID registered, deploy copy synced.
- ✅ SHIPPED: `circletheorems.html` (Circle Theorems, Higher) — draggable prover for all 7 theorems (centre=2×circumference, same segment, semicircle=90°, cyclic quadrilateral, tangent⊥radius, two tangents equal, alternate segment) sharing one drag mechanism (mousedown/mousemove/mouseup + touch); each tab computes live angles/lengths via real vector geometry (verified numerically: 120°=2×60°, tangent-chord=alternate inscribed angle, etc., holds both at default AND after simulated drag) plus a 7-card theorem gallery. Completes after dragging in ≥5 of 7 tabs. Foundation-tier divergence flagged inline. Node wired live, progress ID registered, deploy copy synced.
- ✅ SHIPPED: `combinedevents.html` (Tree & Venn Diagrams) — probability tree for 2 picks from a bag (red/blue count sliders, with/without-replacement toggle correctly changes 2nd-branch denominators, live leaf probabilities verified 0.6×0.6=0.36 etc., 3 combined-event presets: same colour / different / at-least-one-red); Venn diagram sandbox (2 overlapping circles, SVG mask+clipPath per set-notation button — A, B, A∩B, A∪B, onlyA, onlyB, A′, B′, neither — verified A∩B correctly clips to just the lens overlap) with editable region counts feeding a live "pupils in [notation] = N out of total" readout. Completes after ≥1 preset click + exploring ≥3 notations. Node wired live, progress ID registered, deploy copy synced.
- ✅ SHIPPED: `groupeddata.html` (Histograms & Cumulative Frequency) — grouped frequency table with unequal class widths (distance-to-school dataset, one row highlighted: same frequency as two others but double the width); histogram with a WRONG/CORRECT mode toggle sharing one dataset — verified visually that bars 1/3/4 render IDENTICALLY tall in WRONG mode (height=frequency) despite bar 4 being twice as wide, then correctly halves in CORRECT mode (height=frequency density) — the classic misconception made visible; cumulative frequency curve with a draggable handle (mousedown/mousemove/mouseup) computing live interpolated readings, Q1/median/Q3 presets verified exactly against hand calculation (6.0/10.0/18.3 km), box plot drawn from the same reads, IQR calc shown; a numeric quiz (enter median + IQR, tolerance-checked) gates completion, matching the spec's "read the median + IQR correctly" criterion. All dynamic SVG text avoided per the number-line lesson — axis ticks/labels are literal (dataset is fixed), only bar heights/handle position/drag readouts use holes (as safe attribute-level bindings or plain HTML side-panels, never as SVG <text> content). Node wired live, progress ID registered, deploy copy synced.

### ALL 6 GAP-FILL TOPICS COMPLETE — Math L1–L3 now has full syllabus coverage
Every node on the Math map through L3 is now either live (orange, links to a working interactive page) or intentionally deferred to L4/L5. The original 11-topic build plus these 6 gap-fill topics (ratio, seqineq, transform3d, circletheorems, combinedevents, groupeddata) together cover all 6 official GCSE/National 5 strands: Number, Algebra, Ratio & Proportion, Geometry & Measures, Probability, Statistics. L4/L5 (AS/A2, Higher/Advanced Higher) topics remain benched — specs for those are further up this section, unchanged, ready whenever the next phase begins.

## BIOLOGY & CHEMISTRY L3 SYLLABUS AUDIT (2026-07-12) — 1 clear gap each, ✅ BUILT & LIVE
Same method as the physics audit above, against AQA GCSE + National 5. Unlike physics (3 gaps), biology and chemistry L3 were already substantially complete (each maps ~1:1 with its AQA topic list). One clear gap each, now built, verified, wired live, deploy copies synced, progress IDs registered:
- ✅ **BIOLOGY — `cell-transport.html`** (Transport in & out of cells). Animated diffusion (dye spreads & settles), osmosis membrane with a solute slider (water flows to the sugary side), the potato required-practical graph (mass % vs concentration, isotonic crossing at conc 4 — verified), and a three-route comparison (diffusion/osmosis/active transport). Nodes `inout2` (L2) + `celltransport3` (L3) wired to it.ne genuine, grep-confirmed structural gap in each:
- **BIOLOGY — Transport in & out of cells** (diffusion, osmosis, active transport; AQA Cell Biology, has a required practical). Grep found NO "diffusion/osmosis/active transport/magnification" anywhere in `public/biology/`. Added benched nodes `inout2` (L2 "In & out of cells (diffusion)") + `celltransport3` (L3 "Transport in & out of cells") → future `cell-transport.html`. Edges: cells→inout2→celltransport3, cellbio→celltransport3, celltransport3→celltrans(L4). Map re-verified (L2/L3 rows, no overlap). Deploy copy synced.
  - Build spec: L2 intro — particles spread from high→low concentration (drop dye in water sim). L3 — FIG osmosis: a partially-permeable membrane with a water-level slider showing net water movement toward higher solute; potato-in-sugar-solution required-practical (mass % change vs concentration, find isotonic point where line crosses zero); FIG active transport (root hair / gut) moving particles LOW→HIGH using energy, contrasted with diffusion. Optional: microscopy magnification calc (image size = actual × magnification) — folds in here. Completion: reach the isotonic point + contrast the three transport types.
- **CHEMISTRY — Crude oil & polymers** (fractional distillation, cracking, alkenes→addition polymers, combustion of fuels; AQA Organic + Using Resources). Grep found NO "crude oil/fractional distillation" and only passing "polymer" mentions in `public/chemistry/`. Added benched nodes `fuels2` (L2 "Fuels & crude oil") + `crudeoil3` (L3 "Crude oil & polymers") → future `crude-oil.html`. Edges: earth→fuels2→crudeoil3, organic1→crudeoil3, crudeoil3→organic2(L4). Deploy copy synced.
  - Build spec: L2 intro — where fuels come from (crude oil = ancient plankton, a finite resource). L3 — FIG fractionating column (drag a "boiling range" marker up/down the tower; short chains condense high/cold, long chains low/hot — label each fraction: refinery gas, petrol, kerosene, diesel, bitumen, with the trend: longer chain → higher bp, more viscous, less flammable); FIG cracking (a long alkane splits into a shorter alkane + an alkene); FIG addition polymerisation (ethene monomers "unzip" their double bond and link into poly(ethene) — drag n monomers, watch the chain form). Completion: order the fractions by chain length/bp + build a polymer from monomers.
  - Minor placement note (NOT a gap): qualitative reversible reactions / equilibrium / Le Chatelier is GCSE core but currently sits at L4 (`equilibria.html`, teaches Le Chatelier via Haber). Could optionally be surfaced as an L3 entry later; left as-is for now.

## PHYSICS L3 SYLLABUS AUDIT (2026-07-12) — 3 gaps found, ✅ ALL BUILT & LIVE
✅ BUILT: `elasticity.html` (Hooke's-law apparatus + live force–extension graph with limit of proportionality + elastic-PE area, verified), `moments.html` (balance-beam principle-of-moments with F×d readouts + spanner demo, verified 4×3=6×2), `thermal-energy.html` (specific-heat sandbox E=mcΔθ verified 23.8°C + latent-heat heating curve with flat plateaus). All wired to their L1/L2 intro + L3 nodes, deploy copies synced, progress IDs registered.

Audited the physics map's L3 band against the real GCSE (AQA 8463) + National 5 physics specs. The 9 existing L3 nodes (motion/Newton, momentum, energy, waves/EM, resistance-power-mains, radioactivity, particle-model-pressure, motors/generators, space) cover most of it, BUT three required L3 sub-topics were present NOWHERE on the site (grep-confirmed: no "Hooke", "spring constant", "moment/turning force", "specific heat", "latent heat" anywhere in `public/physics/`):
1. **Forces & elasticity — Hooke's law** (force–extension, spring constant k, F = ke, elastic potential energy; AQA required practical). Genuine gap.
2. **Moments & turning forces** (moment = force × perpendicular distance, principle of moments, levers & gears quantitatively; AQA "Forces"). The L1 `simple-machines` page is only a qualitative levers/pulleys intro — no quantitative moments. Gap.
3. **Thermal energy — specific heat capacity & latent heat** (internal energy, SHC E = mcΔθ, specific latent heat, heating/cooling curves; AQA "Particle model", SQA "Properties of matter"). `pressure.html` covers gas pressure & states qualitatively but not SHC/latent heat. Gap.

Added 6 new benched (white) nodes to `physics.dc.html` (map layout re-verified: L3 now wraps to 3 rows, no band overlap). Each new topic follows the site's **multi-level-page convention** (one page scaffolds from a low-level intro → L3), surfaced on the map as BOTH a lower-level intro node and an L3 node sharing one href once built — exactly how `forces-motion.html` already serves pushes(L1)/fmbasic(L2)/motion3(L3). **Answer to "can they have L1/L2 introductions?": yes — that's the design.**
- `stretchy1` (L1 "Stretchy & springy") + `elastic3` (L3 "Forces & elasticity (Hooke's law)") → future `elasticity.html`
- `balance2` (L2 "Balancing & seesaws") + `moments3` (L3 "Moments & turning forces") → future `moments.html` (L1 intro already exists via `simple-machines`)
- `heat2` (L2 "Heating & cooling") + `thermal3` (L3 "Thermal energy (specific heat)") → future `thermal-energy.html`
- Edges added: stretchy1→elastic3, machines→balance2→moments3, heat2→thermal3, particle3→thermal3, plus forward links elastic3→shm, moments3→rotation, thermal3→thermal(L5).

### Build specs for the 3 new physics pages (not yet built — same recipe as other topic pages: DC + deploy .html copy, register progress IDs `elasticity`/`moments`/`thermal-energy`, wire both the intro node AND the L3 node hrefs on the map, house frame + numbered figures + references + next-links)
- **`elasticity.html`** — L1 intro: drag to stretch a spring, watch it spring back (elastic vs plastic/permanent). L3: FIG force–extension apparatus (hang masses, spring extends, plot F vs e live → straight line through origin = Hooke's law, gradient = spring constant k); slider for k; show the limit of proportionality where the line bends. FIG elastic PE = ½ke² shown as the triangular area under the F–e line. Completion: reach the limit of proportionality + read a spring constant.
- **`moments.html`** — L2 intro: a seesaw — drag a child along each side, see which way it tips (bigger force × bigger distance wins). L3: FIG balance beam with draggable weights on both sides, live "clockwise moment vs anticlockwise moment" readout (force × distance each), balances when equal (principle of moments); a spanner/door example for moment = F × perpendicular distance; gears/levers as force multipliers. Completion: balance the beam 3 times at different distances.
- **`thermal-energy.html`** — L2 intro: heat a beaker, watch a thermometer rise, then a cooling curve. L3: FIG specific-heat sandbox (pick material with its c value, set mass + energy in, compute Δθ via E = mcΔθ; compare water's huge c vs metal's small c — why water heats slowly); FIG heating curve of ice→water→steam showing the two FLAT plateaus where latent heat goes into changing state (not temperature), E = mL. Completion: compute a temperature rise + identify where latent heat acts on the curve.

### Scaffold checklist (same recipe as astronomy/earth-science — follow exactly)
1. Pages live in `public/math/`, one `Name.dc.html` per topic + deploy copy `Name.html`. `support.js` already copied.
2. Register topic IDs in `public/lib/progress-store.js` TOPICS as pages ship: `number-line`, `shapes-angles`, `data-graphs`, `fractions-percent`, `algebra-basics`, `geometry-measure`, `coordinates`, `probability`, `quadratics`, `trigonometry`, `statistics`, `ratio`, `seqineq`, `transform3d`, `circletheorems`, `combinedevents`, `groupeddata`, `sequences-series`, `exp-logs`, `trig-advanced`, `functions-calculus`, `vectors`, `distributions`, `mechanics-kinematics`, `calculus`, `numerical-methods`, `normal-hypothesis`, `complex-proof` (labels/levels per table below). NOTE: the 6 new L3 gap topics use their map ids directly as filenames (`ratio.html`, `seqineq.html`, `transform3d.html`, `circletheorems.html`, `combinedevents.html`, `groupeddata.html`) for consistency with the `href`s already wired in `math.dc.html`'s `topics` array.
3. Wire each shipped topic in `math.dc.html`: add `href: "<id>.html"` to its node in the `topics` array (node turns orange automatically) and, if desired, move its card from the planned list into a live list (copy the earth-science map's `liveList` card markup — planned cards are intentionally non-links).
4. Every page: `markVisited(id)` on mount, `markCompleted(id, {via})` on the stated completion criterion; house frame (breadcrumb nav → hero → interactives → theory blocks → `// REFERENCES & FURTHER READING` → next-links per §4 → footer); figure captions `// FIG. 0N —` / `// GRAPH 0N —` per §1; arrows ≥2px/≥10px heads, hit targets ≥44px, diagram labels ≥9px per §2.
5. Interaction house patterns to reuse: PLAY/PAUSE ▶/❚❚ buttons (orange when idle, ink when playing), sliders with `accent-color`, score/streak "field log" panels for games, click-to-select cards with orange `#e8590c` active state, dark `#0a0e1a`/`#070a14` canvases for figure SVGs.

### Levels & dependency edges (as drawn on the map)
- L1: number-line, shapes-angles, data-graphs
- L2: fractions-percent, algebra-basics, geometry-measure, coordinates, probability
- L3: quadratics, trigonometry, statistics
- L4 (AS / Higher): sequences-series, exp-logs, trig-advanced, functions-calculus, vectors, distributions, mechanics-kinematics
- L5 (A2 / Advanced Higher): calculus, numerical-methods, normal-hypothesis, complex-proof
- Edges: number-line→{fdp, algebra} · shapes→geometry · data-graphs→{prob, stats} · fdp→prob · algebra→{coords, quadratics} · coords→quadratics · geometry→trig · prob→stats · quadratics→{funccalc, sequences, explogs, mechanics} · trig→{vectors, funccalc, trigadv} · stats→distributions · funccalc→{calculus, numerical, complex} · distributions→hypothesis · trigadv→calculus · sequences→complex
- The statistics STRAND runs data-graphs (L1) → probability (L2) → statistics (L3) → sampling & distributions (L4) → normal distribution & hypothesis testing (L5) — full coverage of the A-level applied-stats paper. Mechanics is the other applied paper; deeper mechanics (forces, moments, projectiles) already lives in Physics forces-motion — cross-link rather than duplicate.
- **Strand colour-coding (on the map, keep when wiring live nodes):** every node has a 5px `border-left` in its strand colour + legend chips in the header — Number & Algebra `#7048a8` · Geometry & Trig `#2a6fb0` · Graphs & Calculus `#b0472e` · Stats & Probability `#2f7d3a` · Mechanics `#c99a2e`. Strand assignments are the `s:` field on each topic in `math.dc.html`; row order keeps strands roughly columnar (algebra left → geometry → graphs/calculus → stats → mechanics right). Topic pages should reuse their strand colour as a secondary accent (e.g. figure eyebrows) so the strand identity carries through.

### Per-topic interactive specs (the point of each page — build these, not text walls)

**L1 `number-line` — "Number & the Number Line"**
- FIG 01 zoomable number line: pan by dragging, ×10 zoom in/out buttons (≥44px). Zooming between 3 and 4 reveals 3.1, 3.2…; zoom again reveals 3.14…; panning left of 0 reveals negatives. Narration panel updates by context ("between any two numbers there are always more"). Tick labels ≥9px, re-labelled per zoom level. COMPLETE: zoom in ≥2 levels AND visit a negative number.
- FIG 02 fraction-bar builder: denominator slider 1–12 splits a bar; click segments to shade numerator. Live triple readout fraction/decimal/%. Ghost bars flash when the shaded amount equals a simpler fraction (2/4 → 1/2) — teaches equivalence.
- FIG 03 place-value machine: digit dials for Th/H/T/U · tenths/hundredths; ×10 / ÷10 buttons animate digits sliding one column left/right (digits move, the decimal point never does — say so).

**L1 `shapes-angles` — "Shapes & Angles"**
- FIG 01 angle estimator game: random angle drawn (arms ≥2px, arc marker); player sets a 0–180° slider estimate, REVEAL shows true value + error; score by closeness, streak for ≤5° off. Name label (acute/right/obtuse/reflex) updates live with the slider. COMPLETE: 5 estimates within 15°.
- FIG 02 triangle lab: drag any vertex of an SVG triangle; live side lengths + angles; classification label updates (equilateral/isosceles/scalene + right-angled flag); angle-sum readout pinned at 180° with the three angles colour-coded (legend per §1).
- FIG 03 symmetry mirror: pick shape (square/rect/triangle/hexagon/letter H); drag/rotate a mirror line; reflection renders live; correct positions snap + count found lines.

**L1 `data-graphs` — "Data & Graphs"**
- GRAPH 01 build-a-bar-chart: "favourite planet" survey (cross-link astronomy) — tally +/− buttons per category, bars grow live; both axes labelled (Number of pupils / Planet).
- GRAPH 02 misleading-graph detector: same dataset drawn twice — honest 0-based axis vs truncated axis; toggle between them; 3-question "which chart is lying?" mini-quiz. COMPLETE: quiz correct.

**L2 `fractions-percent` — "Fractions, Decimals & Percentages"**
- FIG 01 triple-view converter: 10×10 shading grid + fraction bar + % dial all bound to one value; drag ANY of the three, others follow. Simplified fraction shown (e.g. 35/100 = 7/20).
- FIG 02 percentage-change machine: base price + %-change slider (−50%…+50%); shows multiplier method (+15% → ×1.15); CHAIN button applies a second change to demonstrate +10% then −10% = ×0.99 ≠ 0.
- FIG 03 snap game: cards show 1/4, 0.25, 25%, 3/5, 0.6, 60%… click matching pairs; score/streak field log. COMPLETE: 6 pairs matched.

**L2 `algebra-basics` — "Algebra & the Balance"** (flagship candidate)
- FIG 01 balance-scales solver: equation (e.g. 3x + 2 = 11) rendered as x-bags + unit weights on a pivoting SVG beam. Operation buttons: −1 / +1 / ÷ / × applied to BOTH sides (legal, beam stays level, step logged) — plus a deliberately available "remove from one side only" that tips the beam and bounces back with a warning. Equation history builds down the side to x = 3. 3 difficulty tiers (x+a=b · ax+b=c · ax+b=cx+d, bags on both pans). COMPLETE: solve 3 equations.
- FIG 02 function machine: pipeline (×3 → +2) with animated number token; INVERSE mode reverses the pipeline right-to-left (−2 → ÷3); guess-the-rule game from 3 input/output pairs.

**L2 `geometry-measure` — "Angles, Area & Perimeter"**
- FIG 01 parallel-lines playground: two parallel lines + draggable transversal; the 8 angles colour-coded in matching pairs, live degree labels; toggle highlights alternate / corresponding / co-interior pairs with names (legend per §1).
- FIG 02 area dissection: parallelogram with a shear slider — drag toward 0 shear and it becomes a rectangle, area readout constant throughout (that's the proof); second view: triangle as half a parallelogram; base/height sliders.
- FIG 03 fence puzzle: 24 m of fence, drag rectangle width; perimeter fixed, live area readout + running best; find the 6×6 max. COMPLETE: find max area.

**L2 `coordinates` — "Coordinates & Straight Lines"**
- GRAPH 01 y = mx + c playground: m and c sliders; line redraws live with gradient triangle drawn ON the line (rise/run values labelled); axes labelled with units per §1. Hidden-line game: match a target line shown as two marked points; feedback per slider.
- FIG 02 coordinate battleships: 4-quadrant grid, click to fire at (x, y) called out in words — drills "along the corridor, up the stairs" order. COMPLETE: sink 3 ships.

**L2 `probability` — "Probability"**
- FIG 01 spinner lab: build a spinner by dragging sector boundaries; theoretical P per colour updates as fractions; SPIN ×1 / ×100 / ×1000 buttons; experimental results bar chart converges on theory — narration names the law of large numbers. COMPLETE: run ≥1000 spins.
- FIG 02 two-dice heatmap: ROLL buttons fill a 6×6 sample-space grid heatmap + sum distribution histogram; the 7-diagonal highlights to answer "why is 7 the most common?". Axes labelled (die A / die B / frequency).

**L3 `quadratics` — "Quadratics & Graphs"**
- GRAPH 01 parabola playground: a, b, c sliders on y = ax² + bx + c; live curve + tracked badges for roots (on x-axis), vertex, y-intercept; factorised and completed-square forms displayed and updating when they exist; discriminant readout narrates 2/1/0 roots.
- FIG 02 projectile game: ball thrown along a parabola (cross-link physics forces-motion); adjust launch height/angle sliders to hit a target — solving a quadratic by feel. COMPLETE: hit 3 targets.
- FIG 03 area model of expansion: (x+2)(x+3) as a 2×2 rectangle grid (x², 3x, 2x, 6) with draggable x; grid areas labelled.

**L3 `trigonometry` — "Pythagoras & Trigonometry"**
- FIG 01 Pythagoras proof: right triangle with draggable legs; actual squares drawn on all three sides, live areas; a² + b² = c² readout always true; PLAY runs the classic rearrangement animation.
- FIG 02 SOH-CAH-TOA explorer: right triangle, drag θ; opposite/adjacent/hypotenuse colour-labelled; sin/cos/tan value bars update live; mini-game "you know [opp], you want [hyp] — which ratio?" (3 buttons). COMPLETE: 5 correct ratio picks.
- FIG 03 measure-a-building: street scene; set distance-to-building + angle-of-elevation sliders; tan computes the height, worked line shown.

**L3 `statistics` — "Statistics"**
- GRAPH 01 averages lab: ~12 draggable dots on a dot plot; mean (drawn as a balance/see-saw pivot), median, mode, range all update live with colour-coded markers (legend per §1). Drag one dot far right: mean chases the outlier, median barely moves — narration calls it. COMPLETE: create an outlier gap of mean vs median.
- GRAPH 02 scatter & correlation: draggable point cloud; live best-fit line + strength/direction description ("strong negative"); GUESS-r game (estimate correlation, reveal).
- FIG 03 box-plot builder: same dots from GRAPH 01 collapse into a box plot with min/Q1/median/Q3/max labelled — one dataset, two views.

**L4 `sequences-series` — "Sequences & Series"**
- FIG 01 staircase builder: choose arithmetic (a, d sliders) or geometric (a, r sliders); terms drawn as a bar staircase; nth-term formula updates live; drag n slider to jump to term 50.
- FIG 02 sum-to-infinity: geometric bars (|r| < 1) stack into a container of exact size a/(1−r); r slider — push |r| ≥ 1 and the stack visibly overflows (diverges, narrated). COMPLETE: find a converging and a diverging series.

**L4 `exp-logs` — "Exponentials & Logarithms"**
- GRAPH 01 growth race: linear +5/day vs doubling ×2/day on shared labelled axes; day slider; log-scale toggle straightens the exponential (that's the point — say it).
- FIG 02 the "what power?" machine: log as the inverse function machine of FIG-02-style pipelines (10^? = 1000 → 3); base selector 2/10/e.
- FIG 03 log-scale slider: one slider spanning 1→1,000,000 on a log scale, landmarks (pH, earthquake magnitude, sound dB) — cross-link chemistry acids.

**L4 `trig-advanced` — "Radians & Trig Identities"**
- FIG 01 unit circle: drag point P around the circle; sin/cos read as y/x of P (colour-coded); the sine and cosine WAVES trace live on an adjacent labelled graph as you drag; radians shown as arc length (1 rad marked).
- FIG 02 identity visual: right triangle inside the unit circle shows sin²θ + cos²θ = 1 as live Pythagoras; CAST/quadrant sign explorer.
- FIG 03 solve-a-trig-equation: sin x = 0.5 as a horizontal line crossing the wave — every intersection is a solution; window slider shows infinite solutions.

**L4 `functions-calculus` — "Functions & Differentiation"**
- GRAPH 01 gradient explorer: cubic-ish curve, draggable point P; h-slider slides second point Q toward P — secant visibly becomes the tangent; live gradient readout; as you drag P the derivative curve plots itself point-by-point on a second set of axes below (both axes labelled).
- FIG 02 transformer: base f(x) graph; sliders for f(x)+a, f(x+a), a·f(x) morph it live with ghost of the original.

**L4 `vectors` — "Vectors"**
- FIG 01 vector playground: drag arrows (≥2px shafts, ≥10px heads) tip-to-tail on a grid; resultant auto-draws in orange; column-vector readouts; scalar-multiple slider stretches/flips a vector.
- FIG 02 treasure hunt: navigate a grid to treasure by entering/choosing column vectors; path draws as chained arrows. COMPLETE: reach 3 treasures.

**L4 `distributions` — "Sampling & Distributions"**
- FIG 01 Galton board: balls drop through a peg triangle (animated, PLAY ×1/×100); bins fill into the binomial histogram; rows slider n=4→12; p-bias slider tilts the pegs (skew appears). Narration: each bounce is a coin flip → binomial.
- FIG 02 sampling lab: a population of 200 dots with a true mean line; DRAW SAMPLE n=5/20/80 button plots each sample's mean as a tick; small samples scatter widely, big samples cluster — sample size buys certainty. COMPLETE: ≥10 samples at two different n.

**L4 `mechanics-kinematics` — "Mechanics: Kinematics"**
- GRAPH 01 velocity-time builder: drag segment endpoints of a v-t graph (accelerate/cruise/brake); AREA under it shades live = displacement readout; gradient readout = acceleration; both axes labelled with units (m/s, s) per §1.
- FIG 02 suvat solver: pick the 3 knowns (chips), the right equation highlights itself and computes the unknown; linked animation of a car matching the numbers. Cross-link physics forces-motion for forces/projectiles — don't duplicate.

**L5 `calculus` — "Integration & Differential Equations"**
- GRAPH 01 Riemann rectangles: curve with n-slider (1→200 rectangles); under/over-estimate toggle; total area readout converges to exact value shown as target line; narration "the limit IS the integral". COMPLETE: n ≥ 100.
- FIG 02 accumulation: as a point sweeps x along f(x), the shaded area-so-far plots as a new curve — the antiderivative traces itself (fundamental theorem, shown not told).
- FIG 03 slope field: dy/dx = ky field of small tangent ticks; click a start point and the solution curve threads through the field; k slider. (AH/A2 differential equations, flag inline.)

**L5 `numerical-methods` — "Numerical Methods"**
- FIG 01 Newton-Raphson: curve with a root; click a starting x₀ — tangent drawn, hops to x₁, repeats with animated steps + iteration table; pick a bad start and watch it shoot off (failure case is the lesson).
- FIG 02 interval bisection: [a,b] bracket visibly halves each step toward the root; step counter vs accuracy readout. COMPLETE: pin a root to 3 d.p. with each method.

**L5 `normal-hypothesis` — "Normal Distribution & Hypothesis Testing"**
- GRAPH 01 bell-curve lab: μ and σ sliders reshape the curve (axes labelled); drag two x-bounds to shade a region — live probability %; 68/95/99.7 preset buttons.
- FIG 02 claim-on-trial: scenario ("the factory says mean = 500 g"); draw a sample, its mean lands on the null distribution with the tail shaded — p-value readout and a plain-English verdict slider (5% significance). Hypothesis testing as "how surprising is this if nothing changed?". COMPLETE: reach both verdicts across scenarios.

**L5 `complex-proof` — "Complex Numbers & Proof"**
- FIG 01 Argand playground: drag z₁ and z₂ in the plane; readouts a+bi and mod/arg; SHOW z₁×z₂ demonstrates multiply = rotate + scale (arg adds, mod multiplies) with arc annotations.
- FIG 02 induction dominoes: a row of dominoes; two independent toggles — "base case: knock over domino 1" and "step: each domino knocks the next". PLAY runs the chain; either toggle off and the chain visibly fails at the right spot. That's proof by induction in one picture.

### Formula sheet page — LIVE (`math/formula-sheet.html`)
Interactive version of the official formula provision, linked from the map header ("THE FORMULA SHEET →"). ~33 formula cards, each with: the formula (multi-formula cards use one line per formula via `white-space: pre-line` + `\n`), symbol glossary, "use it when" trigger, strand colour edge, and a per-course status badge. Course selector: GCSE (AQA·Edexcel·OCR, coordinated identical provision) / SQA N5 / A-level / SQA Higher. Status values per formula in the `st` map: `sheet` (GCSE exam aid) / `list` (SQA formulae list) / `booklet` (A-level booklet) / `question` (given in the question — GCSE sphere/cone/pyramid/curved-surface/suvat per DfE Appendix 3) / `memorise` / `na`. Filters: given vs memorise + strand. 2026 policy verified: England's GCSE sheets now provided for the lifetime of current specs; OCR embeds formulae in questions rather than a separate sheet.
**Coverage rule for Opus:** every formula card names its owning topic page ("EXPLAINED IN: … (coming soon)"). When building a topic page: (1) explain each owned formula — symbols, when-to-use, one worked interactive example; (2) change the card's `(coming soon)` note into a real link to the page; (3) if a formula has no natural home (arc/sector → geometry-measure; compound interest → fractions-percent; compound measures + suvat → mechanics-kinematics), it's listed under that topic already — keep ownership as assigned in the `_formulas()` array. Statuses marked uncertain in copy (suvat at A-level varies by board) should be re-verified against the current-year booklet before flipping links live.

### Cross-links & board notes
- quadratics FIG 02 ↔ physics forces-motion (projectiles); data-graphs survey ↔ astronomy; probability/statistics ↔ biology ecology sampling — use in-page "Related:" secondary next-links per §4.
- Level divergence to flag inline per house rules: completing the square & calculus are Higher/A-level-track; N5 vs GCSE trig conventions (3-figure bearings later); vectors are GCSE-Higher/N5-plus; complex numbers are **Further Maths** at A-level but **core** at Advanced Higher (say so on the page); mechanics & stats are the two A-level applied papers.
- Suggested build order (dependency-safe, one page per session): algebra-basics → number-line → coordinates → quadratics → trigonometry → probability → fractions-percent → statistics → remaining L1/L2 → trig-advanced → funccalc → sequences/exp-logs → distributions/mechanics → L5.

## Astronomy — L1–L3 COMPLETE (2026-07-06)
New `public/astronomy/` folder (own `support.js` copy). Homepage Astronomy card now LIVE → `astronomy/astronomy.html`. New map + 5 topic pages, each a self-contained DC (no new `.js`), house pattern (breadcrumb → title → interactive(s) → theory → §3 references → §4 forward next-links → footer), completion tracking via `progress-store`, deploy `.html` copies synced. All 5 IDs added to `progress-store.js` TOPICS. Figures numbered `// FIG. 0N —`/`// GRAPH 0N —` per §1; arrows/strokes ≥2px per §2.
- `astronomy.dc.html` — world map, same layout engine as earth-science/biology. 8 nodes across L1–L5; L1–L3 live orange (incl. shared `../physics/space.html` at L3), L4–L5 bench (Our Dynamic Universe, Astrophysics).
- **L1** `solar-system` — animated orbit view (8 planets circle the Sun, PLAY/PAUSE, click any world or the Sun → stats + fact panel; Saturn drawn with ring) + true-relative-size comparison SVG (all 8 to one scale under the Sun's limb) + moons & Pluto/dwarf-planet notes. Completes after exploring 6 bodies.
- **L1** `day-night` — day/night sim (spin Earth via slider or auto, "you" dot on the surface, live local-time clock + day/sunrise/night state) + Moon-phase orbit sim (walk the Moon round, top-down lit-half view + the phase disc as seen from Earth, 8 named phases, waxing=right-lit / waning=left-lit — geometry screenshot-verified) + solar vs lunar eclipse diagrams. Completes after seeing day, night, new moon & full moon.
- **L2** `scale-orbits` — true-distance "rocket" slider (0–30 AU, live distance in AU/km + light-travel-time + nearest planet, honest linear AU scale showing how empty space is) + orbital-speed-vs-distance GRAPH (click planets, sqrt-x scale) explaining why outer planets are slow & have long years + AU explainer. Completes when rocket reaches the outer edge.
- **L2** `stars` — constellation tracer (Plough / Orion / Cassiopeia / Cygnus over a starfield, each with meaning + how-to-find) + star-colour↔temperature slider (2,500–30,000 °C, colour ramp red→blue, live OBAFGK M class + the class strip highlighting current) + brightness/distance & light-year notes. Completes after tracing 3 constellations + sweeping the temperature range.
- **L3** `the-universe` — cosmic-timeline scrubber (7 epochs Big Bang→today, 13.8 Gyr, epoch colour/glyph/starfield changes as you drag; stable seeded dots to avoid flicker) + expanding-universe interactive (drag/PLAY, every galaxy recedes from "us", redshift/CMB explainer) + 3 galaxy-shape cards (spiral/elliptical/irregular, inline SVGs) + cosmic-address zoom-out strip. Completes at the "today" end of the timeline.
- Notes: galaxy cards were initially built with an `svg` string in a `{{ hole }}` (would render as literal text) — rebuilt as explicit inline `<svg>` per card. Moon-phase disc uses signed elliptical-terminator arcs; verified New/First-Quarter/Full/Last-Quarter render correctly. `day-night` completion moved off render-time `state` mutation to an instance `_seen`/`_done` field.
- ⬜ Verify: spot-checked map, solar-system, day-night (phases), the-universe (timeline + galaxies) rendering clean. Astronomy L4–L5 (Our Dynamic Universe, Astrophysics) remain on the bench. Only **Math** now lacks a subject map.

## Earth Science — L1–L3 COMPLETE (2026-07-05)
New `public/earth-science/` folder (own `support.js` copy). Homepage Earth Science card now LIVE → `earth-science/earth-science.html`. New map + 7 topic pages, each a self-contained DC (no new `.js`), house pattern (breadcrumb nav → title → interactive(s) → theory → §3 references → §4 forward next-links → footer), completion tracking via `progress-store`, deploy `.html` copies synced. All 7 IDs added to `progress-store.js` TOPICS. All wired into the new map (live orange nodes + liveList). Every figure numbered `// FIG. 0N —` per §1; arrows ≥2px stroke / ≥14px heads per §2.
- `earth-science.dc.html` — world map, reuses the biology/chemistry map layout engine (flow-wrap per level, 0-overlap). 12 nodes across L1–L5; L1–L3 live orange (incl. shared `../chemistry/atmosphere-climate.html` at L3), L4–L5 bench (geological time, weather systems, plate-tectonics detail, climate science).
- **L1** `rocks-soil` — rock-family reference (igneous/sedimentary/metamorphic, CSS-texture swatches as the visual clue) + "rock detective" sort game (8 real rocks, score/streak, completes after 5 IDs) + clickable soil-profile (O/A/B/C/R horizons) + rock-uses cards.
- **L1** `weather` — SVG orbit sim: drag Earth around the Sun, fixed axial tilt, lit hemisphere + N/S season + daylight hrs + midday-Sun height update live (science verified: Jan→N winter, axis away from Sun); direct-vs-slanted-ray explainer; weather-type explorer (6 types). Completes after viewing all 4 seasons.
- **L2** `rock-cycle` — clickable circular cycle diagram (5 nodes Magma/Igneous/Sediment/Sedimentary/Metamorphic + 5 process arrows, click node OR arrow → detail panel) + "follow one grain" PLAY/STEP stepper (green marker travels the loop). Completes after the grain visits all 5 forms.
- **L2** `weather-climate` — animated water-cycle SVG (droplets loop through evaporation→condensation→precipitation→collection, vapour vs liquid colour; PLAY/PAUSE) + clickable stage cards + "weather or climate?" sort game (8 statements, score, completes at 5 correct) + equator→pole climate-zone explorer.
- **L2** `earths-structure` — concentric cross-section (crust/mantle/outer core/inner core, drawn to scale, leader-line labels, click a ring → depth/temp/state/composition panel) + thickness depth-ladder + "how we know = seismic waves" + apple-skin analogy. Completes after all 4 layers explored.
- **L3** `plate-tectonics` (flagship) — animated boundary sandbox: pick DIVERGENT / CONVERGENT / TRANSFORM, then slider/PLAY drives the plate motion and builds the landform (divergent: ridge + magma-fill + new crust, side view; convergent: subduction slab + trench + growing volcano + fold mountains + quake foci, side view; transform: map-view fault with offset road + opposite-motion arrows + quakes). Live detail panel (features, earthquake/volcano risk, real example) + convection driver + Ring of Fire + Wegener note. Completes after viewing all 3 boundary types. All 3 geometries screenshot-verified.
- **L3** `rock-cycle-detail` — relative-dating puzzle: click cliff-face events (5 sedimentary beds + 1 igneous intrusion) oldest→youngest, checked against superposition + cross-cutting (completes on correct full order L1→L2→L3→I→L4→L5) + radiometric-dating CALC (100-atom parent/daughter grid, half-life slider, C-14/K-40/U-238 isotope selector → live % parent + computed rock age) + formation-processes cards (grain size, clastic/chemical/organic, contact/regional, index fossils).
- ⬜ Verify: spot-checked map, plate-tectonics (all 3 boundaries), weather orbit, earths-structure, rock-cycle, rock-cycle-detail rendering clean (no console errors). Earth Science L4–L5 (geological time & dating, weather systems & climatology, plate-tectonics detail, climate science) remain on the bench.
